package com.automation;

import io.cucumber.java.*;
import org.openqa.selenium.OutputType;
import org.openqa.selenium.TakesScreenshot;
import org.openqa.selenium.WebDriver;

import java.io.IOException;

public class TestListener {

    /*
    private WebDriver driver = Standalone.setUp();

    public TestListener() throws IOException {

    }

    @BeforeAll
    public static void beforeSuite() throws IOException {

    }

    @Before
    public void beforeScenario(Scenario scenario) {

    }

    @After
    public void afterScenario(Scenario scenario) {

        if (scenario.getSourceTagNames().contains("@retainBrowser")) {
            // Do not quit the WebDriver if the @retainBrowser tag is present
            System.out.println("Retaining WebDriver as @retainBrowser tag is present.");
        } else {
            // Quit the WebDriver if the tag is not present
            Standalone.tearDown();
        }
    }

    @AfterAll
    public static void afterAll() {
        Standalone.tearDown();

    }*/
}
